# Protected verification

The verifier creates only `dev` and `prod` workspaces inside copied runtime roots, checks producer and consumer addresses per workspace, compares each complete environment/VPC/subnet contract, requires no-op reruns, and checks the production instance guardrail.
