# Protected verification

The verifier checks its dynamic-backend negative control, the optional S3 boundary, exact producer and consumer state addresses, exact consumed values, and a final no-op consumer plan.
