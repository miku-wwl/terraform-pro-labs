# Protected verification

The verifier checks its dynamic-backend negative control, the optional S3 boundary, two distinct producer states, exact producer and consumer addresses, exact consumed values, and a final no-op consumer plan for both fixtures.
