# Lab 24：敏感输入与安全输出

## 任务

数据库密码必须在运行时提供，不能设置默认值，也不能写入配置、README、fixture 或日志。

连接 URI 和包含密码的完整数据库配置必须保持敏感；仅包含用户名与“密码是否已配置”这类不泄露密码的信息，可以作为普通输出。移除任何会直接输出原始密码的调试路径。

## 约束

- 不解除密码或连接 URI 的敏感标记。
- 不为绕过敏感值检查而把所有输出都标记为敏感。
- 不编辑验证脚本。
- 只能编辑 `starter/main.tf`。
