# Lab 22：先创建后销毁的替换顺序

## 任务

1. 保留现有替换触发器和资源地址。
2. 配置重叠替换，使 plan 先创建新的服务记录，再删除旧记录。
3. 对未更改的输入保留 no-op plan，并让仅名称变更保持为原地更新。
4. 确保替换计划的动作顺序为先创建、后销毁。



## 约束

- 不要移除或削弱 `triggers_replace`。
- 不要将 release 变更转换为原地更新。
- 不要更改受保护的验证器。
- 保持该 Lab 不使用 provider。



## 可编辑文件

- `starter/main.tf`
