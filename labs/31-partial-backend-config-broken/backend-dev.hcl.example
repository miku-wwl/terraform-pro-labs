# Placeholder only. Copy to backend-dev.hcl and replace the bucket before an explicitly authorized real init.
bucket = "REPLACE_WITH_DEV_STATE_BUCKET"
key    = "network/dev.tfstate"
region = "us-east-1"
