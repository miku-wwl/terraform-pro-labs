{
  "schema_version": 1,
  "id": 29,
  "title": "Sequential Terraform Test State",
  "type": "terraform-test",
  "tier": "quality",
  "difficulty": "hard",
  "estimated_minutes": 30,
  "execution": {"mode": "local-test", "terraform_version": ">= 1.6, < 2.0", "requires_cloud_credentials": false, "creates_billable_resources": false, "backend": "test-managed local state"},
  "validation": {
    "starter_expected_result": "fail",
    "expected_failure_stage": "test",
    "expected_error_category": "sequential-test-coverage",
    "expected_failing_test": "starter/tests/release_flow.tftest.hcl",
    "expected_failure_marker": "EXPECTED_SEQUENTIAL_TEST_FLOW_INCOMPLETE",
    "solution_expected_result": "pass",
    "commands": [
      {"name": "format", "stage": "format", "argv": ["terraform", "fmt", "-check", "-recursive"]},
      {"name": "init", "stage": "init", "argv": ["terraform", "init", "-backend=false", "-input=false"]},
      {"name": "validate", "stage": "validate", "argv": ["terraform", "validate"]},
      {"name": "sequential terraform test", "stage": "test", "argv": ["{python}", "../scripts/verify_test_depth.py"]}
    ]
  },
  "editable_paths": ["starter/tests/release_flow.tftest.hcl"],
  "protected_paths": ["lab.yaml", "starter/main.tf", "starter/versions.tf", "scripts/"]
}
