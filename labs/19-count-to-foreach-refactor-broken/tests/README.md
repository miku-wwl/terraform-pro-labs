# Protected verification contract

The seed establishes the exact old addresses `bucket[0]`, `bucket[1]`, and `bucket[2]`. The verifier
requires exact logical-key mappings to `logs`, `assets`, and `archive`, zero create/delete actions,
identity continuity, the exact final state list, and a final no-op plan.
