{
  "schema_version": 1,
  "id": 6,
  "title": "Provider Requirements, Aliases, and Authentication Boundary",
  "type": "aws-mock",
  "tier": "aws-wiring",
  "difficulty": "medium",
  "estimated_minutes": 25,
  "execution": {"mode": "aws-mock", "terraform_version": ">= 1.6, < 2.0", "requires_cloud_credentials": false, "creates_billable_resources": false, "backend": "local"},
  "validation": {
    "starter_expected_result": "fail",
    "expected_failure_stage": "test",
    "expected_error_category": "provider-selection-and-auth",
    "expected_failing_test": "tests/public.tftest.hcl and scripts/verify_providers.py",
    "expected_failure_marker": "EXPECTED_PROVIDER_ALIAS_AUTH_INCOMPLETE",
    "solution_expected_result": "pass",
    "commands": [
      {"name": "format", "stage": "format", "argv": ["terraform", "fmt", "-check", "-recursive"]},
      {"name": "init", "stage": "init", "argv": ["terraform", "init", "-backend=false", "-input=false"]},
      {"name": "validate", "stage": "validate", "argv": ["terraform", "validate"]},
      {"name": "mock provider behavior", "stage": "test", "argv": ["{python}", "../scripts/verify_providers.py"]}
    ]
  },
  "editable_paths": ["starter/main.tf"],
  "protected_paths": ["lab.yaml", "starter/versions.tf", "tests/", "scripts/"]
}
