{
  "schema_version": 1,
  "id": 17,
  "title": "Data Sources and Cross-Stack Lookup Boundaries",
  "type": "backend-remote-state",
  "tier": "operations-state",
  "difficulty": "medium",
  "estimated_minutes": 20,
  "execution": {"mode": "local-fixture", "terraform_version": ">= 1.6, < 2.0", "requires_cloud_credentials": false, "creates_billable_resources": false, "backend": "local fixture only"},
  "validation": {
    "starter_expected_result": "fail",
    "expected_failure_stage": "test",
    "expected_error_category": "cross-stack-data-boundary",
    "expected_failing_test": "tests/public.tftest.hcl",
    "expected_failure_marker": "EXPECTED_CROSS_STACK_LOOKUP_INCOMPLETE",
    "solution_expected_result": "pass",
    "commands": [
      {"name": "format", "stage": "format", "argv": ["terraform", "fmt", "-check", "-recursive"]},
      {"name": "init", "stage": "init", "argv": ["terraform", "init", "-backend=false", "-input=false"]},
      {"name": "validate", "stage": "validate", "argv": ["terraform", "validate"]},
      {"name": "fixture lookup tests", "stage": "test", "argv": ["{python}", "../scripts/verify_lookup.py"]}
    ]
  },
  "editable_paths": ["starter/main.tf"],
  "protected_paths": ["lab.yaml", "starter/versions.tf", "fixtures/", "tests/", "scripts/"],
  "backend": {
    "example_files": [],
    "init_metadata_paths": ["starter/.terraform"],
    "real_init_opt_in": false
  }
}
